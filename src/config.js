/**
 * @readonly
 */
const config = {
  // The game dimensions in square tiles.
  rows: 31,
  columns: 28,

  // The size in pixels of each tile.
  tileSize: 32,

  offsetSize: 5,

  // The teleporter location
  teleport: {
    row: 14,
    leftToRight: 0,
    rightToLeft: 27,
  },
}

export default config